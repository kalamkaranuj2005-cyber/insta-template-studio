# Set this in Vercel project settings (Environment Variables) to the Render
# backend URL, e.g. https://insta-template-studio-api.onrender.com
# Leave empty for local dev — Vite proxy handles /api and /static.
VITE_API_BASE=
